import type { Metadata, Viewport } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/providers/theme-provider";
import { TooltipProvider } from "@/components/ui/tooltip";

const inter = Inter({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
};

export const metadata: Metadata = {
  title: "SustainIQ - Industrial Carbon Intelligence Platform",
  description: "Track emissions, trade carbon credits, and ensure compliance with AI-powered carbon management for manufacturing industries.",
  keywords: ["carbon tracking", "sustainability", "ESG", "carbon credits", "emissions monitoring", "compliance"],
  authors: [{ name: "SustainIQ" }],
  openGraph: {
    title: "SustainIQ - Industrial Carbon Intelligence Platform",
    description: "Track emissions, trade carbon credits, and ensure compliance with AI-powered carbon management.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} antialiased`}>
        <ThemeProvider
          attribute="class"
          defaultTheme="light"
          enableSystem={false}
          disableTransitionOnChange
        >
          <TooltipProvider>
            {children}
          </TooltipProvider>
        </ThemeProvider>
      </body>
    </html>
  );
}
